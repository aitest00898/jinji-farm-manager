# Ambient Real-Model Capability Matrix — 2026-08-27

## Scope and hard stop

This experiment was authorized for Production 3B only, with exactly three
cases and three attempts per case.  No Prompt, semantic contract, model,
token budget, Prefilter, Reconcile behavior, Production deployment, LINE
operation, D1 business write, Queue operation, Candidate write, or source
consume was performed.

```text
MODEL = @cf/meta/llama-3.2-3b-instruct
TEMPERATURE = 0
MAX_TOKENS = 1536
REAL_MODEL_CALL_LIMIT = 9
ORACLE = NOT_RUN
PRODUCTION_DEPLOYMENT = NOT_DONE
```

The adapter used the exact request generated by `ambient.ts` and forwarded it
through an explicitly gated ephemeral `wrangler dev --remote` session.  The
remote endpoint only forwards `messages`, `max_tokens=1536`, and
`temperature=0` to `env.AI.run`; it has no D1, Queue, Candidate, or LINE
operation.

## Preflight and execution result

The remote preview printed `Ready`, but requests to its local proxy returned
Cloudflare `503` with bounded error code `1105`, including an intentionally
invalid request that should have been rejected before `env.AI.run`.  This
establishes a remote-preview/session transport failure, not a model semantic
result.

The adapter made all nine permitted attempts and did not retry any one:

| Attempt | Case | Transport | Provider result | Semantic result |
| --- | --- | --- | --- | --- |
| A1 | D03_ALONE | FAIL: remote 503/1105 | NOT_OBSERVED | NOT_OBSERVABLE |
| A2 | D03_ALONE | FAIL: remote 503/1105 | NOT_OBSERVED | NOT_OBSERVABLE |
| A3 | D03_ALONE | FAIL: remote 503/1105 | NOT_OBSERVED | NOT_OBSERVABLE |
| B1 | D05_D06 | FAIL: remote 503/1105 | NOT_OBSERVED | NOT_OBSERVABLE |
| B2 | D05_D06 | FAIL: remote 503/1105 | NOT_OBSERVED | NOT_OBSERVABLE |
| B3 | D05_D06 | FAIL: remote 503/1105 | NOT_OBSERVED | NOT_OBSERVABLE |
| C1 | FULL_SELECTED | FAIL: remote 503/1105 | NOT_OBSERVED | NOT_OBSERVABLE |
| C2 | FULL_SELECTED | FAIL: remote 503/1105 | NOT_OBSERVED | NOT_OBSERVABLE |
| C3 | FULL_SELECTED | FAIL: remote 503/1105 | NOT_OBSERVED | NOT_OBSERVABLE |

The runner's bounded reports recorded `aiTransport=FAIL`, no usage values,
zero parsed decisions, and no snapshot.  A separate no-AI preflight reproduced
the same `503 / error code 1105`; therefore no provider inference response was
available to score.  The nine adapter attempts consume this round's hard
budget as required by the experiment rule; no additional inference is
permitted in this round.

## Case aggregation

```text
D03_ALONE_PASS = NOT_OBSERVABLE (3/3 transport failures)
D03_ALONE_JSON_PASS = NOT_OBSERVABLE
D03_ALONE_UNKNOWN_QUANTITY_PASS = NOT_OBSERVABLE

D05_D06_PAIR_PASS = NOT_OBSERVABLE (3/3 transport failures)
D06_SUPPORT_PASS = NOT_OBSERVABLE
D05_D06_DEDUPE_PASS = NOT_OBSERVABLE

FULL_BATCH_PASS = NOT_OBSERVABLE (3/3 transport failures)
FULL_BATCH_6_OF_6 = NOT_OBSERVABLE
FULL_BATCH_D03_PASS = NOT_OBSERVABLE
FULL_BATCH_D06_PASS = NOT_OBSERVABLE
```

No model-capability, support-capability, batch-interference, or flakiness
classification is valid from this run:

```text
BATCH_INTERFERENCE = INCONCLUSIVE
D03_MODEL_CAPABILITY_RISK = INCONCLUSIVE
SUPPORT_MODEL_CAPABILITY_RISK = INCONCLUSIVE
PRIMARY_FINDING = REAL_MODEL_EXECUTION_BLOCKED_BY_REMOTE_DEV_503_1105
RECOMMENDED_ESCALATION_LEVEL = INCONCLUSIVE
```

In particular, this is not evidence for `0/3` model failure.  Provider
inference was not observed.

## Safety evidence

The preflight and matrix path contained no business lifecycle call.  The local
read-only D1 seam throws on any attempted write.  Existing Production safety
snapshot remained unchanged:

```text
DEV-SMOKE-8 = 8 locked / 8 available / 8 buffered / 0 processed
Candidate write = 0
Buffer consume = 0
Official write = 0
LINE send = 0
```

```text
NO_PRODUCTION_D1_WRITE = PASS
NO_BUFFER_CONSUME = PASS
NO_CANDIDATE_WRITE = PASS
NO_OFFICIAL_WRITE = PASS
NO_LINE_SEND = PASS
```

## Next step

Do not modify Prompt or semantic behavior based on this blocked experiment.
First establish an approved real-model transport that can return a normal
Worker response without Production deployment or business bindings.  After a
separate authorization, rerun the exact same 9-call matrix once; do not add
attempts to this round.

## Direct REST transport recovery attempt

This is a separate transport attempt after the remote-preview evidence above;
the earlier `wrangler dev --remote` result is retained unchanged.  The new
path used the Cloudflare native REST endpoint for the same Production model and
did not invoke a Worker, D1, Queue, LINE, Candidate, or business-write path.

```text
WRANGLER_REMOTE_TRANSPORT = BLOCKED_503_1105
DIRECT_REST_ENDPOINT = /accounts/{account_id}/ai/run/@cf/meta/llama-3.2-3b-instruct
ACCOUNT_ID_AVAILABLE = YES
AUTH_TOKEN_AVAILABLE = YES
AUTH_SOURCE = WRANGLER_AUTH
REST_PREFLIGHT_ATTEMPTED = YES
REST_PREFLIGHT_HTTP_STATUS = 200
CLOUDFLARE_SUCCESS = true
PROVIDER_RESPONSE_CONFIRMED = YES
NONEMPTY_RESULT = YES
DIRECT_REST_TRANSPORT = PASS
PREFLIGHT_PROVIDER_CALLS = 1
```

The preflight used only a minimal non-production request.  No response body,
token, prompt, or source text was persisted.  The preflight proves that the
account authentication, native REST endpoint, current model access, and
provider response path are available; it does not measure the semantic matrix.

The subsequent Vitest matrix child was started with the direct REST adapter,
but exited before emitting the bounded matrix marker.  The historical wrapper
did not persist the child exit code or adapter call counter, so the number of
matrix provider attempts is **UNKNOWN**.  It is not safe to infer `0`, `9`, or
any semantic result from the missing marker.  No retry or additional provider
call was made after that failure.  A local-only fake-response check confirmed
the runner/output path itself can emit a marker, but that check is not provider
evidence and is excluded from the matrix.

```text
MATRIX_EXECUTION = FAILED_BEFORE_BOUNDED_REPORT
MATRIX_PROVIDER_CALLS = UNKNOWN
TOTAL_PROVIDER_CALLS = 1 + UNKNOWN_MATRIX_ATTEMPTS
D03_ALONE_PASS = NOT_OBSERVABLE
D05_D06_PAIR_PASS = NOT_OBSERVABLE
FULL_BATCH_PASS = NOT_OBSERVABLE
BATCH_INTERFERENCE = INCONCLUSIVE
D03_MODEL_CAPABILITY_RISK = INCONCLUSIVE
SUPPORT_MODEL_CAPABILITY_RISK = INCONCLUSIVE
REAL_MATRIX_SEMANTIC_RESULT = NOT_OBSERVABLE
PRODUCTION_DEPLOYMENT = NOT_DONE
```

The adapter now uses `AMBIENT_SEMANTIC_EVAL_REST_URL` and
`AMBIENT_SEMANTIC_EVAL_REST_TOKEN`, forwards the exact Production Ambient
request body, rejects a non-Production model, and enforces the nine-call hard
limit.  Its unit tests cover request equivalence, fail-closed provider errors,
secret redaction, model mismatch, and the tenth-call boundary.  The local
suite remained green before the preflight: TypeScript PASS; full Vitest
`534 passed / 1 skipped`.

This round stops without Prompt, contract, model, token-budget, Production
deployment, LINE rerun, or Dev Full Flow changes.  The next valid matrix must
be separately authorized and must first repair the runner so every provider
attempt is emitted as bounded report output before accepting any semantic
measurement.

## Direct REST safety snapshot after the blocked matrix child

The native REST preflight and matrix adapter have no `AmbientEnv`, D1, Queue,
LINE, Candidate, or business-write handle.  A post-run read-only Production
D1 snapshot was taken with separate aggregate SELECTs.  Each returned
`rows_written=0` and `changed_db=false`.

```text
ambient_chat_buffer = 14 total / 8 buffered / 6 processed / 0 expired
ambient_digest_candidates = 1 total / 1 pending
operational_events = 60 total / 13 effective / 47 reversed
abnormal_events = 8 total / 8 active
audit_logs = 66 total
farms = 9 total / 8 production / 1 test
profit_distributions = 12
profit_distribution_allocations = 36
investors = 3
active_locked_dev_sessions = 1
active_dev_cohort_sources = 8
active_dev_sources_buffered = 8
active_dev_sources_processed = 0
```

```text
NO_PRODUCTION_D1_WRITE = PASS
NO_BUFFER_CONSUME = PASS
NO_CANDIDATE_WRITE = PASS
NO_OFFICIAL_WRITE = PASS
NO_LINE_SEND = PASS
DEV_SMOKE_8 = 8 locked / 8 buffered / 0 processed
```

These counts are a current post-run snapshot, not a destructive reconciliation
or a claim that an unavailable pre-call database snapshot was recreated.  They
confirm the expected unchanged Dev cohort boundary and no write rows from the
direct REST evaluation path.

## Current stop state

```text
REST_PREFLIGHT = PASS
DIRECT_REST_TRANSPORT = PASS
REAL_MODEL_SEMANTIC_MATRIX = NOT_OBSERVABLE
MATRIX_RUNNER_FAILURE = BOUNDED_REPORT_NOT_EMITTED
READY_FOR_ARCHITECTURE_DECISION = NO
READY_FOR_DEV_SMOKE_RERUN = NO
READY_FOR_DEV_FULL_FLOW = NO
```

Do not rerun the matrix in this round.  Before a future authorization, repair
the developer-only runner to emit a bounded per-attempt report even when a
case-level harness exception occurs; then obtain a fresh explicit budget for
the complete 9-call experiment.

## Reliable direct-REST matrix attempt — 2026-08-27

This is the separately authorized follow-up after the prior marker-less
direct-REST child.  It did not run another preflight, did not use
`wrangler dev --remote`, and did not change the Production prompt, model,
contract, token budget, or semantic behavior.

```text
MATRIX_RUN_ID = 0b1327ef-ac43-43ea-b552-99eb75a49896
MODEL = @cf/meta/llama-3.2-3b-instruct
TEMPERATURE = 0
MAX_TOKENS = 1536
PREFLIGHT_THIS_ROUND = NOT_RUN_BY_POLICY
MATRIX_PROVIDER_ATTEMPTS = 9
PROVIDER_RESPONSES_CONFIRMED = 8
TERMINAL_ATTEMPTS = 8
ORPHAN_ATTEMPTS = 1
AUTOMATIC_RETRIES = 0
```

The write-ahead ledger is:

```text
forensics/runtime/ambient-semantic-eval-attempts-2026-08-27.jsonl
```

The first eight attempts have bounded HTTP 200/provider-response evidence.
They are JSON-valid and normalized, but the Production validator rejected the
model decisions as `invalid_event_schema`; consequently no system event build
or semantic pass was produced.  Their bounded counts were:

| Case | Attempts | Provider responses | JSON-valid | Validation-pass | Bounded decision coverage |
| --- | ---: | ---: | ---: | ---: | --- |
| `D03_ALONE` | 3 | 3 | 3 | 0 | `1/1` each |
| `D05_D06` | 3 | 3 | 3 | 0 | `2/2` each |
| `FULL_SELECTED` C1-C2 | 2 | 2 | 2 | 0 | `4/6` each; missing safe refs `03,06` |
| `FULL_SELECTED` C3 | 1 | unknown | unknown | unknown | orphan after START |

The C3 START was persisted before the transport call, but it has no terminal
record.  The wrapper captured `exitCode=1`, `signal=null`,
`markerSeen=false`, and `stderrClass=NONEMPTY`, then reconstructed the bounded
report from the ledger.  Therefore this run is not a complete 9-result
semantic matrix.  It is valid evidence that the runner now counts the possible
attempt without falsely reporting zero, but it still has an end-to-end terminal
completion failure at C3.

```text
D03_ALONE_SEMANTIC_PASS = NOT_EVALUABLE (3/3 schema-invalid)
D05_D06_SEMANTIC_PASS = NOT_EVALUABLE (3/3 schema-invalid)
FULL_SELECTED_SEMANTIC_PASS = NOT_EVALUABLE (2/2 schema-invalid; C3 orphan)
BATCH_INTERFERENCE = INCONCLUSIVE
D03_MODEL_CAPABILITY_RISK = INCONCLUSIVE
SUPPORT_MODEL_CAPABILITY_RISK = INCONCLUSIVE
NEW_CONTRACT_IMPLEMENTATION_BUG = NOT_ESTABLISHED
```

The bounded ledger contains no raw prompt, completion, transcript, LINE
identifier, credential, or response fragment.  No production binding was
available to the child process, and the post-run Production D1 snapshot stayed
unchanged: Ambient buffer `14 total / 8 buffered / 6 processed`, candidates
`1 pending`, operational `60 total / 13 effective / 47 reversed`, abnormal
`8 active`, audit `66`, and the locked Dev cohort `8 buffered / 0 processed`.

```text
NO_PRODUCTION_D1_WRITE = PASS
NO_BUFFER_CONSUME = PASS
NO_CANDIDATE_WRITE = PASS
NO_OFFICIAL_WRITE = PASS
NO_LINE_SEND = PASS
PRODUCTION_DEPLOYMENT = NOT_DONE
READY_FOR_DEV_SMOKE_RERUN = NO
READY_FOR_DEV_FULL_FLOW = NO
```

## Event-schema forensic follow-up — 2026-08-27

The eight confirmed provider responses from this matrix were reviewed without
any new AI call. All eight have bounded HTTP 200/provider-response evidence,
JSON pass, normalization pass, and the run-level validation class
invalid_event_schema. The ninth FULL_SELECTED attempt remains an orphan after
its write-ahead START; its provider response is unknown.

The historical ledger does not contain raw completion, parsed decision
objects, decision kinds, field keys, field types, or validation issue paths.
Its validDecisionCount values (1, 2, and 4) are accounted-source counts, not
schema-valid decision counts. Therefore the exact invalid field cannot be
reconstructed safely:

~~~text
CONFIRMED_PROVIDER_RESPONSES = 8
JSON_PASS = 8/8
EVENT_SCHEMA_PASS = 0/8
INVALID_EVENT_SCHEMA = 8/8
PRIMARY_EVENT_SCHEMA_FAILURE = UNKNOWN
SYSTEMATIC_MODEL_CONTRACT_VIOLATION = INCONCLUSIVE
D03_EVENT_SCHEMA_VALID = UNKNOWN
D05_SIMPLE_EVENT_SCHEMA_VALID = UNKNOWN
D06_SUPPORT_SCHEMA_VALID = UNKNOWN
~~~

The effective validator is checkAmbientSelectedSourceCoverage
(src/ambient.ts:2275-2397); its event checks cover canonical type, bounded
non-empty raw, confidence enums, quantity confidence enum, abnormal-null
quantity, bounded numeric quantity, and optional bounded text fields. The
value-free diagnostic helper inspectAmbientDecision
(src/ambient.ts:1494-1532) now projects only allowlisted keys, type classes,
missing-key flags, enum validity, quantity nullability, raw presence, and
support-target status. This new diagnostic path is developer-only and is not
a backfill of the eight historical responses.

The old schema does not show a deterministic D03 or D06 contradiction:
abnormal + quantity=null + quantityConfidence=unknown is representable, and
support + targetRef to a selected event is representable. Because the
historical field values are unavailable, model capability, support capability,
and batch interference remain inconclusive. No Prompt, model, semantic
contract, production behavior, or deployment change was made.

~~~text
NEW_AI_CALLS = 0
PRODUCTION_DEPLOYMENT = NOT_DONE
READY_FOR_MINIMAL_SCHEMA_FIX = NO
READY_FOR_PROMPT_FIX = NO
READY_FOR_MODEL_COMPARISON = NO
READY_FOR_ARCHITECTURE_REDESIGN = NO
READY_FOR_DEV_SMOKE_RERUN = NO
READY_FOR_DEV_FULL_FLOW = NO
~~~

Local verification after adding bounded diagnostics and future orphan
terminalization:

~~~text
TypeScript = PASS
Full Vitest = 45 files; 544 passed, 1 skipped
SCHEMA-DIAG tests = PASS
RUNNER-TERM tests = PASS
~~~

See
forensics/ambient-real-model-event-schema-forensic-2026-08-27.md for the
complete field-level evidence boundary and validator audit.
